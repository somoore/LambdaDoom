# AGENTS.md

Operating guide for coding agents in this repo.

The full, current guide lives in **[CLAUDE.md](CLAUDE.md)** — read that. It covers what
LambdaDoom is (native DOOM in an AWS Lambda MicroVM, streamed to a browser tab), the
capsule, the verified Lambda MicroVMs API facts, the Fork B proxy, the commands, and the
conventions. AGENTS.md intentionally does not duplicate it, so there is one source of
truth and nothing drifts.
